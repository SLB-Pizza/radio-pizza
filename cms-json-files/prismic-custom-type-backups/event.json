{
  "Main": {
    "uid": {
      "type": "UID",
      "config": {
        "label": "Event Unique ID",
        "placeholder": "Enter meaningful unique id (no spaces, hyphenate-words-like-this) >> example-event-unique-id"
      }
    },
    "main_event_image": {
      "type": "Image",
      "config": {
        "constraint": {},
        "thumbnails": [],
        "label": "Main Event Image"
      }
    },
    "event_name": {
      "type": "StructuredText",
      "config": {
        "single": "heading1",
        "label": "(REQUIRED) Event Name",
        "placeholder": "Add the event name here..."
      }
    },
    "event_blurb": {
      "type": "StructuredText",
      "config": {
        "multi": "paragraph, heading4, heading5, heading6, strong, em, hyperlink, list-item, o-list-item",
        "allowTargetBlank": true,
        "label": "(REQUIRED) Event Blurb",
        "placeholder": "Add a blurb describing the event here..."
      }
    },
    "event_start": {
      "type": "Timestamp",
      "config": {
        "label": "(REQUIRED) Event Start",
        "placeholder": "Set the event's start date and time..."
      }
    },
    "event_end": {
      "type": "Timestamp",
      "config": {
        "label": "Event End",
        "placeholder": "Set the event's end date and time..."
      }
    },
    "event_header_button_text": {
      "type": "Text",
      "config": {
        "label": "Event Header Button Text",
        "placeholder": "What action can a listener take here? ('Buy Tickets', 'RSVP (Free)',  etc.)"
      }
    },
    "event_header_button_link": {
      "type": "Link",
      "config": {
        "allowTargetBlank": true,
        "label": "Event Header Button Link",
        "placeholder": "Add a link to complete that action (ticketing site, venue's RSVP site, etc.)"
      }
    },
    "event_location": {
      "type": "Text",
      "config": {
        "label": "Event Location Name",
        "placeholder": "Please enter the name of the location (e.g. Radio City Music Hall)"
      }
    },
    "event_location_physical_address": {
      "type": "Text",
      "config": {
        "label": "(IN-PERSON ONLY) Event Location Physical Address",
        "placeholder": "If in-person, please enter the location's physical address (e.g 1260 6th Ave, New York)"
      }
    },
    "event_location_link": {
      "type": "Link",
      "config": {
        "allowTargetBlank": true,
        "label": "Event Stream Link",
        "placeholder": "If the event has a livestream, add that link here (YouTube, Twitch, etc.)"
      }
    },
    "event_livestream_embed": {
      "type": "StructuredText",
      "config": {
        "multi": "paragraph, heading3, strong, em, hyperlink, embed",
        "allowTargetBlank": true,
        "label": "Event Livestream Embed",
        "placeholder": "For YouTube livestreams, select Embed button, then copy/paste the link. For all other links, select the Hyperlink, then copy/paste the link in with desired label text."
      }
    },
    "event_mixes": {
      "type": "Group",
      "config": {
        "fields": {
          "event_mix": {
            "type": "Link",
            "config": {
              "select": "document",
              "customtypes": ["mix"],
              "label": "Event Mix",
              "placeholder": "Add a mix from this event..."
            }
          }
        },
        "label": "Event Mixes"
      }
    },
    "event_residents": {
      "type": "Group",
      "config": {
        "fields": {
          "event_resident": {
            "type": "Link",
            "config": {
              "select": "document",
              "customtypes": ["resident"],
              "label": "Event Resident",
              "placeholder": "Add a resident that featured in this event..."
            }
          }
        },
        "label": "Event Residents"
      }
    }
  }
}
