import React from 'react'

/**
 * Renders
 * @category Layout Helper
 * @function LiveRadioInfoDisplay
 * @param {Object.<String>} globalMarqueeData
 * @returns {jsx}
 */
export default function LiveRadioInfoDisplay({ globalMarqueeData }) {
  return
}
