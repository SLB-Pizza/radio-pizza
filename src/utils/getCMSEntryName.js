/**
 * Function that takes a CMS entry node object and returns a string; the title of the entry as listed in the CMS.
 * @function getCMSEntryName
 * @param {Object} node
 * @returns {String} the name of the CMS Entry as found in the CMS
 */
function getCMSEntryName(node) {
  return 'getCMSEntryName'
}

export default getCMSEntryName
